{
  "name": "ticktick-mcp-scripts",
  "private": true,
  "description": "Workflow scripts for ticktick-mcp — tests and linting",
  "scripts": {
    "test": "uv run pytest -m 'not integration'",
    "test:integration": "uv run pytest -m integration",
    "lint": "uv run ruff check . && uv run mypy src/"
  }
}
